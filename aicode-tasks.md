# aicode-auto task list — one task per line:
#   <comma-separated files> | <plain-English task; say "add a test">
# Blank lines and lines starting with # are ignored.
# aicode-auto works top to bottom, committing each success, stopping if one needs a human.
#
# 2026-07-17 overnight batch: the correlation-engine chain (blueprint steps 1-3).
# Depends on existing types: Signal (Sources/AiOSCore/Signal.swift), SignalKind/SpokeID
# (Identifiers.swift), SignalValue (SignalValue.swift), Insight/Citation/Reliability
# (Insight.swift, Citation.swift), AnalyticsEngine/AnalysisResult (AnalyticsEngine.swift).

Sources/AiOSCore/SignalGrouping.swift,Tests/AiOSCoreTests/SignalGroupingTests.swift | Create a public enum `SignalGrouping` with three pure static functions over the existing `Signal` type (Signal has `let id: UUID`, `let source: SpokeID`, `let kind: SignalKind`, `let payload: [String: SignalValue]`). Functions: `groupByKind(_ signals: [Signal]) -> [SignalKind: [Signal]]`; `groupBySource(_ signals: [Signal]) -> [SpokeID: [Signal]]`; and `groupByPayloadKey(_ signals: [Signal], key: String) -> [String: [Signal]]` which groups signals by the string value of `payload[key]` (use SignalValue's `stringValue`) and OMITS any signal whose payload lacks `key` or whose value has no stringValue. Preserve input order within each bucket. Add a test file with @Test cases covering: mixed kinds and sources yield correct bucket counts, empty input yields empty dictionaries, and payload-key grouping omits signals missing the key. Add a test.

Sources/AiOSCore/CorrelationRule.swift,Tests/AiOSCoreTests/CorrelationRuleTests.swift | Create a `public protocol CorrelationRule: Sendable { func evaluate(_ signals: [Signal]) -> [Insight] }`. Add a `public struct ThresholdRule: CorrelationRule` with `public init(kind: SignalKind, minCount: Int, severity: InsightSeverity = .medium)`. Its evaluate: let matched = signals filtered to `.kind == kind`; if `matched.count >= minCount` return ONE Insight built as `Insight(citations: matched.map { Citation(signalID: $0.id, source: $0.source, snippet: "\($0.kind) signal") }, title: "…", severity: severity, reliability: .confirmed)`; otherwise return []. (Insight.init: `Insight(citations: [Citation] = [], sourceSignalIDs: [UUID]? = nil, title: String, detail: String = "", severity: InsightSeverity = .info, reliability: Reliability = .cantVerify, posture: RecommendationPosture? = nil, confidence: Double = 1.0, ...)`. Citation.init: `Citation(id: UUID = UUID(), signalID: UUID, source: SpokeID, field: String? = nil, snippet: String)`.) Add tests: below threshold -> []; at/above threshold -> exactly one Insight whose citations.count equals the matched count, whose reliability is .confirmed, and whose canAssertAsFact is true. Add a test.

Sources/AiOSCore/RuleBasedAnalyticsEngine.swift,Tests/AiOSCoreTests/RuleBasedAnalyticsEngineTests.swift | Create a `public struct RuleBasedAnalyticsEngine: AnalyticsEngine` with a `public let rules: [any CorrelationRule]` and `public init(rules: [any CorrelationRule])`. Implement `public func analyze(_ signals: [Signal]) async throws -> AnalysisResult`: if signals.isEmpty return `.empty`; otherwise run every rule's `evaluate(signals)` in order, concatenate all emitted Insights, and return `AnalysisResult(insights: allInsights)`. (AnalyticsEngine requires `func analyze(_ signals: [Signal]) async throws -> AnalysisResult`; AnalysisResult has `init(insights: [Insight])` and static `.empty`.) Add tests: empty signals -> result equals .empty; an engine built from two ThresholdRules over a crafted batch returns insights whose count equals the sum of both rules' individual outputs. Add a test.
