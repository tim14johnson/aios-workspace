# Dev Requests — Inbox

Drop one bullet per idea. Rough is fine. See `../DEVLOOP.md` for how these get built.

Shape (clearer "Done when" = better local results):
`- [ ] <what you want>. Files: AiOSCore/Sources/AiOSCore/<file>.swift. Done when: <test passes / function exists / behaviour>.`

---

## Inbox

- [ ] _(example — delete me)_ Add a `daysSince(_:)` helper on Date in AiOSCore that returns whole days between two dates. Files: AiOSCore/Sources/AiOSCore/... . Done when: a unit test for same-day (0), 1 day, and 30 days all pass.

## In progress

## Done
